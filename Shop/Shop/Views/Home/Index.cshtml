@model HomeViewModel

<h1>Лучшие товары</h1>
<div class="row mt-3 mb-2">
        @{
            foreach (Light light in Model.favLights)
            {
                @await Html.PartialAsync("AllLights", light)
            }
        }
</div>